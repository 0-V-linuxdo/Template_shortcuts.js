# Archive

Historical materials for `Template_shortcuts.js`.

- `Template_JS/`: archived Template userscript bundle
- `legacy/`: legacy scripts and storage snapshots
- `reference/`: reference HTML captures used during migration work
- `dist/`: old archived `[Template] *.js` bundles
- `assets-unused/`: assets kept for reference but not used by the current release manifest
