{
  "name": "template-shortcuts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "stage:release": "node scripts/stage-release-branch.js",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
